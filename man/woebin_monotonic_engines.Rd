% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/woebin-monotonic.R
\name{woebin_monotonic_scorecard_retry}
\alias{woebin_monotonic_scorecard_retry}
\alias{woebin_monotonic_isoreg}
\title{Numeric engines for monotonic WOE binning}
\usage{
woebin_monotonic_scorecard_retry(
  dt,
  y,
  x,
  breaks = NULL,
  special_values = NULL,
  missing_join = NULL,
  stop_limit = 0.1,
  count_distr_limit = 0.05,
  bin_num_limit = NULL,
  positive = "bad|1",
  no_cores = 2,
  print_step = 0L,
  method = "tree",
  ignore_const_cols = TRUE,
  ignore_datetime_cols = TRUE,
  check_cate_num = TRUE,
  replace_blank_inf = TRUE,
  ...,
  count_distr_step = 0.025,
  verbose = TRUE
)

woebin_monotonic_isoreg(
  dt,
  y,
  x,
  breaks = NULL,
  special_values = NULL,
  missing_join = NULL,
  stop_limit = 0.1,
  count_distr_limit = 0.05,
  bin_num_limit = NULL,
  positive = "bad|1",
  no_cores = 2,
  print_step = 0L,
  method = "tree",
  ignore_const_cols = TRUE,
  ignore_datetime_cols = TRUE,
  check_cate_num = TRUE,
  replace_blank_inf = TRUE,
  ...,
  count_distr_step = 0.025,
  verbose = TRUE
)
}
\arguments{
\item{dt}{A data frame containing the response and predictor.}

\item{y}{Name of the binary response variable.}

\item{x}{Name of one numeric predictor.}

\item{breaks}{Optional numeric break vector for \code{x}.}

\item{special_values}{Optional special-value vector for \code{x}.}

\item{missing_join, stop_limit, count_distr_limit, bin_num_limit, positive, no_cores, print_step, method, ignore_const_cols, ignore_datetime_cols, check_cate_num, replace_blank_inf}{Arguments passed to \code{scorecard::woebin()}.}

\item{...}{Additional arguments passed to \code{scorecard::woebin()}.}

\item{count_distr_step}{Step used by the scorecard retry engine when increasing \code{count_distr_limit}. It is accepted but unused by the isotonic engine.}

\item{verbose}{Logical. Print progress messages.}
}
\value{
One data frame compatible with an element of a \code{scorecard::woebin()} output list.
}
\description{
These functions implement the numeric engines used by \code{woebin_monotonic()}. They share a small interface: one numeric predictor in, one scorecard-compatible bin table out.
}
\details{
\code{woebin_monotonic_scorecard_retry()} repeatedly reruns \code{scorecard::woebin()} with coarser settings until the observed positive rates are monotonic or the retry path is exhausted.

\code{woebin_monotonic_isoreg()} starts from a regular scorecard binning, fits weighted isotonic regressions in both directions to its ordered regular-bin rates, selects the lower-error direction, merges adjacent bins in the resulting isotonic blocks, and rebuilds the WOE table with \code{scorecard::woebin()}.
}
