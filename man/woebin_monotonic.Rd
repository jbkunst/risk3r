% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/woebin-monotonic.R
\name{woebin_monotonic}
\alias{woebin_monotonic}
\title{Monotonic WOE binning with interchangeable numeric engines}
\usage{
woebin_monotonic(
  dt,
  y,
  x = NULL,
  var_skip = NULL,
  type = c("scorecard_retry", "isoreg"),
  breaks_list = NULL,
  special_values = NULL,
  missing_join = NULL,
  stop_limit = 0.1,
  count_distr_limit = 0.05,
  bin_num_limit = NULL,
  positive = "bad|1",
  no_cores = 2,
  print_step = 0L,
  method = "tree",
  ignore_const_cols = TRUE,
  ignore_datetime_cols = TRUE,
  check_cate_num = TRUE,
  replace_blank_inf = TRUE,
  save_as = NULL,
  ...,
  count_distr_step = 0.025,
  verbose = TRUE
)
}
\arguments{
\item{dt}{A data frame containing the response and predictors.}

\item{y}{Name of the binary response variable.}

\item{x}{Optional character vector of predictor names. If \code{NULL}, all variables except \code{y} and \code{var_skip} are used.}

\item{var_skip}{Optional character vector of variables to skip.}

\item{type}{Numeric monotonic engine: \code{"scorecard_retry"} or \code{"isoreg"}.}

\item{breaks_list}{Optional list of manual breaks passed to \code{scorecard::woebin()}.}

\item{special_values}{Optional list of special values passed to \code{scorecard::woebin()}.}

\item{missing_join}{Optional missing-value merge direction passed to \code{scorecard::woebin()}. Use \code{NULL} to keep missing values separate.}

\item{stop_limit, count_distr_limit, bin_num_limit, positive, no_cores, print_step, method, ignore_const_cols, ignore_datetime_cols, check_cate_num, replace_blank_inf}{Arguments passed to \code{scorecard::woebin()}. The isotonic engine starts from this scorecard binning and only merges adjacent regular numeric bins, so its minimum-size and maximum-bin constraints are preserved.}

\item{save_as}{Optional path used to save the returned list as an RDS file.}

\item{...}{Additional arguments passed to \code{scorecard::woebin()}.}

\item{count_distr_step}{Step used by the \code{"scorecard_retry"} engine.}

\item{verbose}{Logical. Print progress messages.}
}
\value{
A named list compatible with \code{scorecard::woebin()}.
}
\description{
\code{woebin_monotonic()} sends categorical variables to regular \code{scorecard::woebin()} and processes each numeric variable with the engine selected in \code{type}. Every numeric engine returns one table compatible with an element of a \code{scorecard::woebin()} result.
}
\details{
The \code{"scorecard_retry"} engine reruns \code{scorecard::woebin()} with increasingly coarse settings until it obtains monotonic observed positive rates or exhausts the retry path.

The \code{"isoreg"} engine first obtains a regular scorecard binning. It then fits weighted increasing and decreasing isotonic regressions to the ordered regular-bin positive rates, selects the direction with the lowest weighted squared error, merges adjacent bins belonging to the same isotonic block, and rebuilds the final scorecard-compatible table from the resulting breaks.

The method does not impose an economic direction. Statistical monotonicity still requires expert review for business and risk meaning.
}
\examples{
if (FALSE) {
data(germancredit, package = "scorecard")

bins <- woebin_monotonic(
  germancredit,
  y = "creditability",
  x = c("duration.in.month", "credit.amount", "purpose"),
  type = "isoreg",
  no_cores = 1,
  verbose = FALSE
)
}
}
