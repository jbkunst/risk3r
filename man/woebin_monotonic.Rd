% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/woebin-monotonic-engine.R
\name{woebin_monotonic}
\alias{woebin_monotonic}
\title{Monotonic WOE binning with interchangeable numeric engines}
\usage{
woebin_monotonic(
  dt,
  y,
  x = NULL,
  var_skip = NULL,
  type = c("scorecard_retry", "isoreg"),
  breaks_list = NULL,
  special_values = NULL,
  missing_join = NULL,
  stop_limit = 0.1,
  count_distr_limit = 0.05,
  bin_num_limit = 8,
  positive = "bad|1",
  no_cores = 2,
  print_step = 0L,
  method = "tree",
  ignore_const_cols = TRUE,
  ignore_datetime_cols = TRUE,
  check_cate_num = TRUE,
  replace_blank_inf = TRUE,
  save_as = NULL,
  ...,
  count_distr_step = 0.025,
  verbose = TRUE
)
}
\arguments{
\item{dt}{A data frame containing the response and predictors.}

\item{y}{Name of the binary response variable.}

\item{x}{Optional character vector of predictor names. If \code{NULL}, all variables except \code{y} and \code{var_skip} are used.}

\item{var_skip}{Optional character vector of variables to skip.}

\item{type}{Numeric monotonic engine: \code{"scorecard_retry"} or \code{"isoreg"}.}

\item{breaks_list}{Optional list of manual breaks. Manual breaks take precedence over the selected numeric engine for that variable.}

\item{special_values}{Optional list of special values passed to \code{scorecard::woebin()}.}

\item{missing_join}{Optional missing-value merge direction passed to \code{scorecard::woebin()}. Use \code{NULL} to keep missing values separate.}

\item{stop_limit, count_distr_limit, bin_num_limit, positive, no_cores, print_step, method, ignore_const_cols, ignore_datetime_cols, check_cate_num, replace_blank_inf}{Arguments compatible with \code{scorecard::woebin()}. For \code{type = "isoreg"}, \code{count_distr_limit} is applied within the regular non-missing, non-special sample and \code{bin_num_limit} limits regular numeric bins.}

\item{save_as}{Optional path used to save the returned list as an RDS file.}

\item{...}{Additional arguments passed to \code{scorecard::woebin()}.}

\item{count_distr_step}{Step used by the \code{"scorecard_retry"} engine.}

\item{verbose}{Logical. Print progress messages.}
}
\value{
A named list compatible with \code{scorecard::woebin()}.
}
\description{
\code{woebin_monotonic()} applies regular \code{scorecard::woebin()} binning to categorical variables and a selected monotonic engine to numeric variables. Each numeric engine receives one variable and returns one table compatible with an element of the \code{scorecard::woebin()} output list.
}
\details{
The \code{"scorecard_retry"} engine uses the existing retry/coarsening strategy. The \code{"isoreg"} engine fits weighted isotonic regressions in both directions, keeps the direction with the smallest weighted squared error, and merges adjacent isotonic blocks until the requested minimum bin share and maximum number of bins are satisfied. Its cut points are then passed to \code{scorecard::woebin()} to produce the final compatible table.

Statistical monotonicity does not guarantee business sense. The selected direction and final bins still require expert review.
}
\examples{
if (FALSE) {
data(germancredit, package = "scorecard")

bins <- woebin_monotonic(
  germancredit,
  y = "creditability",
  x = c("duration.in.month", "credit.amount", "purpose"),
  type = "isoreg",
  no_cores = 1,
  verbose = FALSE
)
}
}
