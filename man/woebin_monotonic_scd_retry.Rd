% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/woebin-monotonic.R
\name{woebin_monotonic_scd_retry}
\alias{woebin_monotonic_scd_retry}
\title{Simple monotonic retry wrapper around scorecard::woebin()}
\usage{
woebin_monotonic_scd_retry(
  dt,
  y,
  x = NULL,
  var_skip = NULL,
  breaks_list = NULL,
  special_values = NULL,
  missing_join = NULL,
  stop_limit = 0.1,
  count_distr_limit = 0.05,
  bin_num_limit = NULL,
  positive = "bad|1",
  no_cores = 2,
  print_step = 0L,
  method = "tree",
  ignore_const_cols = TRUE,
  ignore_datetime_cols = TRUE,
  check_cate_num = TRUE,
  replace_blank_inf = TRUE,
  save_as = NULL,
  ...,
  count_distr_step = 0.025,
  verbose = TRUE
)
}
\arguments{
\item{dt}{A data frame with both x variables and the y variable.}

\item{y}{Name of the response variable.}

\item{x}{Optional character vector with predictor variables. If \code{NULL}, all variables except \code{y} and \code{var_skip} are used.}

\item{var_skip}{Optional character vector with variables to skip.}

\item{breaks_list}{Optional list of break points passed to \code{scorecard::woebin()}.}

\item{special_values}{Optional list of special values passed to \code{scorecard::woebin()}.}

\item{missing_join}{Optional argument passed to \code{scorecard::woebin()}.}

\item{stop_limit, count_distr_limit, bin_num_limit, positive, no_cores, print_step, method, ignore_const_cols, ignore_datetime_cols, check_cate_num, replace_blank_inf}{Arguments passed to \code{scorecard::woebin()}.}

\item{save_as}{Optional path to save the output as an RDS file.}

\item{...}{Additional arguments passed to \code{scorecard::woebin()}.}

\item{count_distr_step}{Numeric step used to increase \code{count_distr_limit} when a numeric variable is not monotonic.}

\item{verbose}{Logical. If \code{TRUE}, prints progress messages.}
}
\value{
A list compatible with the output of \code{scorecard::woebin()}.
}
\description{
\code{woebin_monotonic_scd_retry()} is a small wrapper around \code{scorecard::woebin()}. It follows a simple retry strategy: for numeric variables, retry \code{scorecard::woebin()} by increasing \code{count_distr_limit} until \code{0.2}; if that is not enough, reduce \code{bin_num_limit}.
}
\details{
This is not an optimal binning algorithm. It only attempts to make numeric bins monotonic by making the binning coarser. Missing values are kept as a separate bin and are excluded from the monotonicity check.
}
\examples{
if (FALSE) {
data(germancredit, package = "scorecard")

bins <- woebin_monotonic_scd_retry(
  germancredit,
  y = "creditability",
  x = c("duration.in.month", "credit.amount", "age.in.years"),
  no_cores = 1,
  verbose = FALSE
)

woebin_summary(bins)
}
}
